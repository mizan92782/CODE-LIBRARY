#include <iostream>
#include <cmath>

int main() {
    double x = 16;
    double base = 2;

   // ! logarhim log2(16)=?
   // logathirm of 16 of base 2 ?
    double result = log(x) / log(base);

    std::cout << "Log base " << base << " of " << x << " is: " << result << std::endl;

    return 0;
}
